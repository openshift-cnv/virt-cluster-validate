#!/usr/bin/bash

MESSAGE="FAIL: $*"
echo "$MESSAGE"

if [ -n "$FAIL_FD" ]; then
    # We export the message to safely pass it into the eval block without quote escaping issues
    export _FAIL_MSG="$MESSAGE"
    eval "echo \"\$_FAIL_MSG\" >&${FAIL_FD}"
fi

exit 1
