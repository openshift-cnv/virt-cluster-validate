#!/usr/bin/bash

LVL=$( echo "${1}" | tr "a-z" "A-Z" )
[[ "$LVL" =~ INFO|WARN|FAIL ]] || echo "Unknown level: '$LVL'"
shift 1

MESSAGE="${LVL}: $*"
echo "$MESSAGE"

if [ -n "$TEST_REPORT_FD" ]; then
    export _REPORT_MSG="$MESSAGE"
    eval "echo \"\$_REPORT_MSG\" >&${TEST_REPORT_FD}"
fi
